rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /HGKG/{allPaths=**} {
    	allow read, write: if true;
      allow create, write, update: if true;
    }
    match /{allPaths=**} {
    	allow read, write: if false;
    }
  }
}